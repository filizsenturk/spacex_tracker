query launch($id: ID!){
    launch(id:$id ){
        id
        upcoming
        mission_id
        mission_name
        links {
            flickr_images
            video_link
            reddit_launch

        }
        launch_year
        rocket {
           rocket {
               id
           }
            rocket_name
            rocket_type
        }
    }
}